using RealTimeDataGenerator.models;

namespace RealTimeDataGenerator;

public class Producer
{
    private WorkerConfig _config;
    
    Producer(WorkerConfig config)
    {
        _config = config;
    }
    
    
}